{
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "buildCommand": "pnpm install && pnpm prisma generate && next build",
  "installCommand": "pnpm install",
  "env": {
    "PRISMA_CLIENT_ENGINE_TYPE": "wasm"
  }
}
